import React, { Component } from 'react';

/**
 * Renders the Query page based on the selected schema in the state
 *
 * @author Camilo Ortiz
 */

const QueryBody = ({}) => {
  return(
    <div>
      QueryBody
    </div>
  )
}

export default QueryBody;
