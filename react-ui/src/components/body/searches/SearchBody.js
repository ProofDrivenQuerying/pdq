import React, { Component } from 'react';

/**
 * Renders the Search page based on the selected schema and query in the state
 *
 * @author Camilo Ortiz
 */

const SearchBody = ({}) => {
  return(
    <div>
      SearchBody
    </div>
  );
}

export default SearchBody;
