/** The SQL parser files are for parsing queries that a user inputs or modifies when the GUI is running.
* The other IO files deal with reading schema or query information that is persisted from prior sessions.
**/
